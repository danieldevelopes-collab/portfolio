# build output (regenerated by `npm run build`)
dist/

# build cache: shallow clones of the source repos, re-cloned on demand
.cache/

# dependencies (this project has none at runtime, but just in case)
node_modules/

# firebase
.firebase/
firebase-debug.log
firebase-debug.*.log

# os / editor
.DS_Store
*.log
